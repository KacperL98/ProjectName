import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    padding: 20,
  },
  txtPreviousV: {
    textAlign: 'center',
    fontSize: 16,
    marginBottom: 20,
  },
  textResult: {
    textAlign: 'center',
    fontSize:28,
    color: 'steelblue',
  }
});

export default styles;